version: '3.1'

volumes:
  postgres_data:

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: P@ssw0rd!
    volumes:
      - postgres_data:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
